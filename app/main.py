from app import app

# run the app / starts Flask application

if __name__ == '__main__':
    # debug=True to update code changes automatically, without using F5 in the browser
    app.run(debug=True)
